= form_for(current_user.following_relationships.find_by(followed_id: @user.id), method: :delete, remote: true) do |f|
  = hidden_field_tag :followed_id, @user.id
  = f.submit "フォローを解消", class: "common-button theme-primary l-mini-button message-btn"
